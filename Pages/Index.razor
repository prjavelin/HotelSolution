@page "/"
@using Model
@*
<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.

<SurveyPrompt Title="How is Blazor working for you?" />
*@

<h2 class= "bg-light border p-2">
    First Room
</h2>

Room: @Room.RoomName
<br />

Price: @Room.Price
<br />
<Input type="number" @bind-value="Room.Price" @bind-value:event="oninput"/> <br />

<Input type="checkbox" @bind-value="@Room.IsActive" @bind-value:event="oninput" checked="@(Room.IsActive?"checked": null)"/> &nbsp Is Active
<br />

<span>Room is @(Room.IsActive == false? "inactive" : "active")</span>

<br/>

<select @bind="SelectedRoomProp">
    @foreach(var prop in Room.RoomProps)
    {
        <option value="@prop.Name">@prop.Name</option>
    }
    

</select>

<span>The Room Prop selected is: @SelectedRoomProp</span>

@code{
    string SelectedRoomProp = "";
    List<BlazorRoom> RoomsList = new List<BlazorRoom>();

    BlazorRoom Room = new BlazorRoom()
        {
            Id = 1,
            RoomName = "Villa Suite",
            IsActive = true,
            Price = 499,
            RoomProps = new List<BlazorRoomProp>
            {
                new Model.BlazorRoomProp{Id=1, Name="Sq Ft",Value="100"},
                new Model.BlazorRoomProp{Id=2,Name="Ocupancy", Value="3"}
            }


        };

    protected override void OnInitialized()
    {
        base.OnInitialized();

        RoomsList.Add(new BlazorRoom()
            {
                Id = 201,
                RoomName = "Villa Suite one bed br",
                IsActive = true,
                Price = 399,
                RoomProps = new List<BlazorRoomProp>
            {
                new Model.BlazorRoomProp{Id=1, Name="Sq Ft",Value="100"},
                new Model.BlazorRoomProp{Id=2,Name="Ocupancy", Value="3"}
            }


            });

        RoomsList.Add(new BlazorRoom()
            {
                Id = 202,
                RoomName = "Villa Suite three bed br",
                IsActive = true,
                Price = 699,
                RoomProps = new List<BlazorRoomProp>
            {
                new Model.BlazorRoomProp{Id=1, Name="Sq Ft",Value="150"},
                new Model.BlazorRoomProp{Id=2,Name="Ocupancy", Value="3"}
            }


            });

    }

}